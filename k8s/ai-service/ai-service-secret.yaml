apiVersion: v1
kind: Secret
metadata:
  name: ai-service-secret
  namespace: travelmate
type: Opaque
stringData:
  # Replace with your real key (don’t commit it). You can also `kubectl apply -f -` from your terminal.
  GEMINI_API_KEY: ""

