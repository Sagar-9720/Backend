apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: travelmate
spec:
  selector:
    app: ai-service
  ports:
    - protocol: TCP
      port: 8086
      targetPort: 8086
  type: ClusterIP

